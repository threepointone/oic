this repo holds tests, examples, sandbox scripts for (oia)[threepointone/oia]

(because the generated js references `require('oia')`, it's not possible to run scripts from inside the oia repo. hence, this rigmarole.)